避免AIGC感的要求：

1.不要有“其一、其二， 第一，第二这种”，也不能出现\begin{enumerate}  \item， 分点统一用 (1)、(2)这样编号

2.比如： 

 (1)不仅提高了系统效率，也增强了系统稳定性，同时改善了系统可扩展性。 这种句子太顺、太满、太像模板。可以改成： 该设计主要降低了热数据访问路径的开销，同时使冷数据能够通过 SQLite 进行持久化管理。 重点是写具体，不要把所有好处都堆上去。

 (2) 少用“具有重要意义”“提供了新思路”“奠定了基础”这种空泛结论 这些词不是不能用，而是不能没有支撑地用。比如： 本文的研究对紧凑哈希表的实践应用具有重要意义。 就比较虚。可以改成： 该方案没有完全复现原理论结构中的多层级 cubby，而是将其转化为更容易实现和测试的冷热分层结构，因此更适合作为工程化复现版本。 这样更像自己真的做过系统。 

(3) 少用“有效地”“显著地”“充分地”“进一步地”这类万能副词 比如： 有效地提升了系统的查询效率。 除非你有明确实验数据，否则“有效地”会显得很虚。可以改成： 在热数据命中时，查询只需要访问内存 tail cubby，因此避免了直接访问 SQLite 带来的额外开销。 

(4) 不要每段都写成“背景—问题—方法—意义”的小作文结构 AIGC 常见段落长这样： 随着……的发展，……问题日益突出。为了解决这一问题，本文提出……。实验结果表明……。该方法具有……意义。 论文摘要可以有这种逻辑，但正文里每段都这样就很假。你可以多写一些“实现细节”“取舍理由”“边界情况”，比如： 由于 SQLite 本身更适合作为冷数据存储，而不适合承载频繁迁移的小对象，因此本文没有将每次插入都同步写入数据库，而是先进入内存 tail cubby，再根据容量和访问情况进行下沉。

 (5) 多写“为什么这么设计”，少写“这个设计很好” 比如不要只写： 本文采用 SQLite 存储冷数据，提高了系统的可维护性。 可以改成： 原结构中不同层级 cubby 的迁移和局部重构逻辑较复杂，若直接持久化每一层结构，需要额外维护层级状态和重定位信息。本文将冷数据统一下沉到 SQLite 中，使冷数据管理从结构维护问题转化为键值记录管理问题。 这种就更像本科毕设里真实的工程权衡。

 (6) 避免“过度对称”的句子 比如： 在查询方面，系统通过……；在插入方面，系统通过……；在删除方面，系统通过……。 这不是错，但太像生成式总结。可以改得自然一点： 插入操作不立即访问 SQLite，而是优先写入内存 tail cubby。查询时，系统先根据 MiniArray D 判断目标位置；如果目标仍位于热缓存中，则直接在内存中完成查找，否则再访问 SQLite。删除操作也沿用这一判断过程，从而避免对冷热数据使用两套完全独立的接口。

 (7) 不要用太多“高大上”的抽象词 例如： 鲁棒性、泛化性、可扩展性、可维护性、模块化、低耦合、高内聚、端到端、闭环 这些词可以用，但最好每次都能落到具体实现。比如“可扩展性”后面要说明扩展什么，是扩展存储后端、替换淘汰策略，还是调整热缓存容量。 

(8) 多保留一点“工程不完美感” 完全完美的表述反而像 AIGC。真实论文可以承认设计边界： 需要说明的是，该方案并未完整保留原 k-kick 结构中多层级 cubby 的理论性质，而是侧重于保留哈希置换、Facility 分区和局部路由等核心思想，并将复杂的冷数据层级管理简化为 SQLite 存储。 这类句子很有用，因为它显得你知道自己做了什么、没做什么。

 (9) 摘要里不要塞太多“全面提升” 比如： 提高了查询效率、降低了空间开销、增强了持久化能力、改善了系统稳定性。 这类太满。你的题目更适合强调： 实现复杂度降低 冷热数据分层更清晰 热数据查询路径更短 冷数据获得持久化能力 但理论最优性不作为主要目标

 (10) 句子长短要有变化 AIGC 经常每句话都差不多长，读起来很平。你可以让一些关键句短一点： 原理论结构并不容易直接落地。 主要困难在于 cubby 层级较多，迁移逻辑复杂，持久化状态也不容易维护。 这种比一整句大长句自然很多。

 (11) 少用“本文围绕……展开研究”这种开场 可以用，但别到处用。比如章节开头可以换成： 本章主要说明系统从原 k-kick 结构到冷热分层结构的改造过程。重点不在于重新定义哈希模型，而在于解释哪些理论组件被保留，哪些组件被工程化简化。 比“本文首先介绍系统架构，其次介绍模块设计，最后介绍实现流程”自然。

3.避免，宣传性语言、-ing 肤浅分析、模糊归因、否定式排比、三段式滥用、破折号/粗体/emoji 滥用、内联标题列表、谄媚语气、填充短语、虚假范围、通用积极结论



格式要求：

（1）不要出现cache missing这样的英文词语。英文仅允许用在：代码片段、缩写、原始文献中独创的专有词汇。

（2） 复查参考文献的真实性：文章名字、发表会议、作者名单、DOI链接是不是对的。

（3） 正常人写文章没那么爱加粗。粗体那么多一眼AI。

（4）表格里的性能参数写汉语，表里的标注也不能出现英语

（5）公式都要有编号、算法伪代码 按章节编号、图也按章节编号

（6）引用的参考文献尽量最近今年，至少3篇中文文献，总体至少25篇文献

（7）引用的文献必须要在正文中用到

（8）所有图/表/算法在正文被引用