本文系统围绕论文 *On the Optimal Time/Space Tradeoff for Hash Tables* 的主要结构进行简化复现与落地实现。代码的核心目标不是重新证明理论结果，而是把 Facility、Cubby、Slot、Local Query Router、MiniArray、商压缩、k-kick 插入、tail 管理、多 tier Cubby 重建以及双表渐进迁移串成一个可运行、可测试、可观测的哈希系统。

系统入口是 `include/ht.h` 中的 `HashTable`，主要实现集中在 `src/ht.cpp`。对外接口包括：

- `HashTable::init(const TableParams&)`：初始化表规模、Facility 数量、Router 数组、tail Cubby 与随机置换种子。
- `HashTable::insert(uint64_t)`：插入键，返回是否新插入、Router 访问步数、移动次数和 Cubby tier。
- `HashTable::query(uint64_t)`：查询键，返回是否命中、Router 访问步数和 Cubby tier。
- `HashTable::erase(uint64_t)`：删除键，必要时从 tail 回填元素。
- `HashTable::bulk_load(...)`：批量加载键。
- `HashTable::state()`：返回当前元素数、表规模、局部桶数和 Facility 数量。
- `HashTable::visit_structure(...)`：遍历当前内存结构，用于生成结构快照。
- `HashTable::pi_of(...)`：暴露可逆置换值，用于观测和快照。

## 1. 参数与总体结构

### 1.1 外部参数

外部参数定义在 `include/config.h` 的 `TableParams`：

```cpp
struct TableParams {
  uint64_t n = 10000;
  int k = 2;
  double load_factor = 0.90;
  uint64_t seed1 = 0;
  uint64_t seed2 = 0;
  uint64_t seed3 = 0;
  string storage = "mysql";
  // mysql 连接参数...
};
```

其中：

- `n` 是规模提示，初始化时会被规整到 2 的幂表规模 `N`。
- `k` 用作单 Cubby 内工程版 k-kick 的最大踢出次数，同时当前代码也把它作为 Facility tier 上限的工程参数。
- `load_factor` 用于触发扩容。
- `seed1/seed2/seed3` 参与生成 Feistel 可逆置换的轮密钥。

### 1.2 表规模与 Facility 划分

初始化逻辑位于 `src/ht.cpp` 的 `HashTable::Impl::init`。核心步骤如下：

```python
init(params):
  n_ = 0
  active_.N = next_pow2(max(2, params.n))
  active_.K = choose_K(active_.N)
  facilities_cnt = max(1, active_.N / active_.K)
  active_.facilities.resize(facilities_cnt)

  根据 seed1/seed2/seed3 生成 Feistel 轮密钥

  for each Facility f:
    f.tiers.clear()
    f.max_tier = max(0, params.k)
    f.D = K 个 Router
    f.tail = null
    f.tail_owned.reset()
    ensure_tail(active_, f)
```

`choose_K(N)` 使用近似多对数规模：

```cpp
lg = log2(N)
K = ceil(lg * lg)
K 限制在 [64, 4096]，且 K <= N
K 向下取 2 的幂
```

这样做的原因是：理论结构中每个 Facility 管理 `K = polylog N` 规模的局部范围；工程实现中需要保证 `K` 是 2 的幂，便于后续使用 `g / K` 与 `g % K` 拆分地址。

## 2. 可逆置换与地址拆分

### 2.1 Feistel 可逆置换

可逆置换由 `PermutationHash::pi` 和 `PermutationHash::inverse` 实现，定义在 `src/ht.cpp`。代码采用 3 轮 Feistel：

```python
pi(x):
  将 64 位 x 拆成 L、R 两个 32 位半块
  for i in 0..2:
    newL = R
    newR = L xor F(R, key_i)
    L = newL
    R = newR
  return L || R

inverse(y):
  按相反轮次执行 Feistel 逆过程
```

轮函数 `Feistel_F` 使用异或密钥、乘法混合和右移异或：

```cpp
x ^= k;
x *= 0x9e3779b97f4a7c15ULL;
x ^= x >> 32;
```

该置换承担两个职责：

- 打散键分布，用于定位 Facility、Router 桶和 Cubby 内偏好槽位。
- 与商压缩配合，使系统可以通过 `payload + MetaEntry + 上下文` 恢复 `pi(x)`，再用逆置换恢复原始键。

### 2.2 地址拆分

地址拆分逻辑主要由 `facility_for_key`、`route_bucket_for` 和商压缩相关函数共同完成。给定键 `x`：

```text
gx = pi(x)
g  = gx & (N - 1)      // gx 的低 logN 位
r  = g / K             // Facility 编号
b  = g % K             // Facility 内 Router 桶号
```

代码中：

- `facility_for_key(t, gx)` 使用 `r = (gx & (N - 1)) / K` 选择 Facility。
- `route_bucket_for(t, gx)` 使用 `b = (gx & (N - 1)) % K` 选择 Router。
- `N` 和 `K` 都按 2 的幂处理，保证位运算、取模和整除拆分一致。

## 3. 核心数据结构

### 3.1 HashTable 与 TableState

`HashTable` 对外隐藏实现细节，内部使用 `HashTable::Impl`。`Impl` 中维护：

```cpp
TableState active_;
optional<TableState> old_;
size_t migrate_progress_ = 0;
TableParams params_;
PermutationHash pi_;
uint64_t n_ = 0;
RebuildScheduler scheduler_;
```

`TableState` 是一张逻辑表：

```cpp
struct TableState {
  uint64_t N = 0;
  uint64_t K = 0;
  vector<Facility> facilities;
};
```

其中：

- `active_` 是当前写入和优先查询的表。
- `old_` 在扩缩容迁移期间保存旧表。
- `migrate_progress_` 表示迁移到第几个旧 Facility。

### 3.2 Facility

Facility 定义在 `include/otsh/facility.h`：

```cpp
struct Facility {
  vector<vector<unique_ptr<Cubby>>> tiers;
  int max_tier = 0;

  int tail_tier = 0;
  Cubby *tail = nullptr;
  unique_ptr<Cubby> tail_owned;

  vector<Router> D;
};
```

Facility 的职责：

- 聚合一组不同 tier 的 Cubby。
- 维护唯一允许未满的 `tail` Cubby。
- 保存长度为 `K` 的 Router 数组 `D`。
- 作为后台重建和扩缩容迁移的局部单位。

### 3.3 Cubby 与 Slot

Cubby 定义在 `include/otsh/cubby.h`：

```cpp
struct Cubby {
  int tier = 0;
  size_t capacity = 0;
  size_t size = 0;
  vector<optional<uint64_t>> slots;
  vector<size_t> occupied;
  FreeSlotTree free_slots;
  MiniArray meta;
};
```

其中：

- `slots[i]` 保存商压缩后的 `quotient payload`，不是完整键。
- `meta[i]` 保存 `MetaEntry` 的变长编码。
- `occupied` 保存当前占用槽位下标，便于扫描、迁移和 tail 回填。
- `free_slots` 用于查找可用槽位。
- `tier` 表示 Facility 层的 Cubby 尺寸层级，不等同于 k-kick 的踢出深度。

### 3.4 FreeSlotTree

`FreeSlotTree` 定义在 `include/otsh/free_slot_tree.h`，实现在 `src/free_slot_tree.cpp`。它使用线性化线段树记录区间空闲槽数量：

```cpp
struct FreeSlotTree {
  int capacity = 0;
  vector<uint64_t> bitmaps;
  void build();
  int find_free() const;
  void mark_used(int i);
  void mark_free(int i);
};
```

实现逻辑：

```python
build():
  n = 不小于 capacity 的最小 2 的幂
  叶子 i < capacity 置 1，表示空闲
  自底向上维护每个区间的空闲数

find_free():
  若根节点空闲数为 0，返回 -1
  从根向下优先走左子树
  到叶子后返回槽位下标

mark_used(i):
  将叶子 i 的计数置 0
  沿父节点向上更新

mark_free(i):
  将叶子 i 的计数置 1
  沿父节点向上更新
```

理论中空闲槽结构更强调低深度位图树；当前实现采用线段树式数组，逻辑清楚、易维护，但查找深度为 `O(log capacity)`。

## 4. Router 实现

Router 定义在 `include/otsh/router.h`，实现在 `src/router.cpp`。它对应理论中的 Local Query Router，用于在一个 Facility 的局部桶中维护：

```text
key -> (Cubby*, slot)
```

### 4.1 内部状态

```cpp
class Router {
  struct Entry {
    uint64_t key;
    Value v;       // pair<Cubby*, size_t>
    uint64_t bits; // splitmix64 后的随机比特串
  };

  struct Node {
    int left;
    int right;
    int entry_idx;
    int bucket_idx;
    uint8_t split_bit;
  };

  vector<Entry> entries_;
  vector<Node> nodes_;
  vector<vector<int>> buckets_;
  int root_;
  size_t encoded_bits_;
};
```

`key_bits(key)` 使用：

```cpp
splitmix64(key ^ 0x9e3779b97f4a7c15ULL)
```

为键生成稳定的 64 位伪随机比特串。

### 4.2 查询

`Router::locate` 从 Patricia 风格二叉前缀树根节点开始，按 `split_bit` 读取键的随机比特串，选择左右子树。这里的“前缀树”只用于 Router 内部区分随机比特串，和 Facility 层的 `tier` 尺寸层级不是同一概念。到达叶子后比较原始键；如果多个键的 64 位随机比特串完全相同，则进入碰撞桶线性扫描。

伪代码：

```python
locate(key):
  bits = key_bits(key)
  node = root
  steps = 0

  while node is valid:
    steps++
    if node is collision bucket:
      for entry in bucket:
        steps++
        if entry.key == key:
          return (entry.value, steps)
      return (not_found, steps)

    if node is leaf:
      if entry.key == key:
        return (entry.value, steps)
      return (not_found, steps)

    if bit_at(bits, node.split_bit) == 0:
      node = node.left
    else:
      node = node.right

  fallback scan entries
```

返回值同时包含 `steps`，用于记录 Router 访问步数。

### 4.3 插入、删除与编码统计

`Router::insert` 和 `Router::erase` 都会整体调用 `rebuild()`：

```python
insert(key, value):
  if key already exists:
    overwrite value
  else:
    append Entry
  rebuild()

erase(key):
  remove matching Entry
  rebuild()
```

`rebuild()` 的流程：

```python
rebuild():
  清空 nodes_、buckets_、root_
  为所有 Entry 重新计算 key_bits
  构造 Entry 下标数组
  build_range(idxs, 0, size, 0) 构造 Patricia 风格二叉前缀树
  recompute_encoded_bits()
```

`encoded_bits_` 通过 `BitWriter` 模拟序列化得到，用于逻辑空间统计。它不是实际压缩后运行时内存占用。

### 4.4 与理论的差异

理论中的 Local Query Router 目标是在局部小集合上实现紧凑编码和常数时间动态更新。当前实现保留了以下语义：

- 键通过随机比特串路由。
- 使用前缀分裂结构区分键。
- 记录逻辑编码位数。
- 查询返回候选 Cubby 和 Slot。

替代方式是：

- 不使用查表法处理压缩 bitstream。
- 插入和删除后整体重建 Router 内部的二叉前缀树。
- `bits_total()` 是逻辑编码估算，不等同于真实内存。

## 5. MiniArray 与 MetaEntry

### 5.1 MiniArray

MiniArray 保存 Cubby 内每个槽位的变长元数据。

内部状态：

```cpp
vector<uint32_t> bitlens_;
vector<uint64_t> offsets_;
vector<Bits> entries_;
vector<uint64_t> buf_;
```

含义：

- `bitlens_[i]`：第 `i` 个条目的位长。
- `offsets_[i]`：第 `i` 个条目在连续 bit buffer 中的起始位置。
- `entries_[i]`：未压缩副本。
- `buf_`：重新打包后的连续位缓冲。

更新流程：

```python
update(i, bits, bitlen):
  检查 i 是否越界
  bitlens_[i] = bitlen
  entries_[i] = bits
  repack()

repack():
  根据 bitlens_ 重新计算 offsets_
  分配 buf_
  逐条逐位写入 buf_
```

读取流程：

```python
access(i):
  bl = bitlens_[i]
  start = offsets_[i]
  从 buf_ 中逐位取出 bl 位
  返回 64 位 word 数组
```

理论 MiniArray 使用内存 B 树和查表法支持均摊常数时间更新。当前实现保留“变长条目 + 连续打包 + 位数统计”的接口语义，但用整段重打包替代复杂动态结构。

### 5.2 MetaEntry 字段

```cpp
struct MetaEntry {
  int64_t delta = 0;
  uint32_t mid_bits = 0;
  uint32_t router_bits = 0;
  uint32_t insert_bits = 0;
};
```

字段含义：

- `delta = g_i - slot_index`，用于从实际槽位恢复 Cubby 内偏好槽位。
- `mid_bits = g_k / |I|`，用于恢复局部桶范围内的中间位。
- `router_bits` 是路由相关指纹字段，当前由 `splitmix64(key ^ gx_pi ^ 常量)` 截断得到 16 位。
- `insert_bits` 记录插入过程标签，当前主要保存 k-kick step。

`MetaCodecLayout` 根据 `K` 和 Cubby 容量派生位宽：

```cpp
mid_w = bit_width((K - 1) / cubby_capacity)
rout_w = 16
ins_w = 8
```

编码方式：

- `delta` 使用 zigzag varint。
- `mid_bits`、`router_bits`、`insert_bits` 使用固定宽度 bit 字段。

## 6. 商压缩与键恢复

商压缩相关函数位于 `src/meta_entry.cpp` 和 `src/ht.cpp`：

- `quotient_payload(gx_pi, logN)`
- `make_meta_entry(...)`
- `encode_meta_entry(...)`
- `decode_meta_entry(...)`
- `reconstruct_gx_pi(...)`
- `recover_key_from_slot(...)`
- `slot_pi_matches(...)`
- `put_quotient_slot(...)`

### 6.1 写入槽位

给定键 `x` 和 `gx = pi(x)`：

```python
put_quotient_slot(cubby, slot, key, gx, facility_r, table, ins_tag):
  logN = log2(table.N)
  layout = meta_layout(table.K, cubby.capacity)
  meta = make_meta_entry(key, gx, slot, facility_r, N, K, cubby.capacity, ins_tag)
  bits, bitlen = encode_meta_entry(meta, layout)
  cubby.slots[slot] = gx >> logN
  cubby.meta.update(slot, bits, bitlen)
```

其中 `slots[slot]` 只保存 `gx` 去掉低 `logN` 位后的高位商。

### 6.2 MetaEntry 构造

`make_meta_entry` 的核心逻辑：

```cpp
g_star = gx & (N - 1)
g_k    = g_star & (K - 1)
g_i    = g_k % cubby_capacity

delta    = g_i - slot_index
mid_bits = g_k / cubby_capacity
router_bits = hash(key, gx) 截断 16 位
insert_bits = ins_tag & 0xff
```

注意：代码中 `facility_r` 参数传入 `make_meta_entry`，但当前函数内部没有直接使用它；Facility 编号在恢复阶段作为上下文参与重建。

### 6.3 恢复置换值

`reconstruct_gx_pi` 根据上下文恢复完整 `gx`：

```cpp
reconstruct_gx_pi(facility_r, N, K, cap, slot, payload, meta, logN):
  g_i = slot + meta.delta
  if g_i not in [0, cap): fail

  g_k = meta.mid_bits * cap + g_i
  if g_k >= K: fail

  g_star = facility_r * K + g_k
  if g_star != (g_star & (N - 1)): fail

  gx = (payload << logN) | g_star
  return gx
```

恢复原始键：

```cpp
recover_key_from_slot(cubby, slot, facility_r, table):
  if slot empty: return null
  decode MetaEntry from cubby.meta[slot]
  gx = reconstruct_gx_pi(...)
  return pi.inverse(gx)
```

查询校验：

```cpp
slot_pi_matches(cubby, slot, facility_r, table, query_gx):
  decode meta
  gx = reconstruct_gx_pi(...)
  return gx == query_gx
```

这个设计保证 Router 只给出候选位置，最终命中必须通过 payload + meta + 上下文恢复出的 `pi(x)` 校验。

## 7. 单 Cubby 内的工程版 k-kick 插入

当前代码没有使用 `include/otsh/kkick_tree.h` 中的类实现完整 k-kick 树；该头文件只是保留 `k` 参数语义的占位说明。真正插入逻辑在 `src/ht.cpp` 的 `kkick_insert`。

### 7.1 探针位置

`probe_slot(key, cap, i)`：

```cpp
gx = pi(key)
h  = splitmix64(gx ^ (i * 常量))
pos = h % cap
```

### 7.2 插入流程

伪代码：

```python
kkick_insert(facility, cubby, key, facility_r):
  kmax = max(0, params.k)
  cur_key = key

  for step in 0..kmax:
    pos = probe_slot(cur_key, cubby.capacity, step)

    if cubby.slots[pos] is empty:
      写入 cur_key 的 payload 和 MetaEntry
      occupied.push_back(pos)
      size++
      free_slots.mark_used(pos)
      Router 插入 cur_key -> (cubby, pos)
      return ok(slot=pos, moved=step)

    kicked_key = recover_key_from_slot(cubby, pos, facility_r, active_)
    if recover failed:
      return fail

    Router 删除 kicked_key 的旧记录
    用 cur_key 覆盖 pos
    Router 插入 cur_key -> (cubby, pos)
    cur_key = kicked_key

  free = cubby_find_free(cubby)
  if no free slot:
    return fail

  将 cur_key 写入 free slot
  moved = kmax + 1
  return ok
```

### 7.3 与理论的关系

理论中的 k-kick 树是单 Cubby 内的受控插入与驱逐机制，需要维护更丰富的控制信息，例如深度、饱和状态、合法驱逐对象等。当前实现保留：

- 单 Cubby 内有界踢出。
- 最多执行 `k` 轮探针/踢出。
- 每次踢出同步维护 payload、MetaEntry、Router。
- `kick_count` 记录当前工程路径中的移动次数。

替代方式：

- 用伪随机探针链代替完整 k-kick 树。
- 不维护理论中的深度优先级和饱和状态。
- 超过 `k` 轮后用 `FreeSlotTree` 选择空槽兜底。

因此，`kick_count` 可以用于观测当前实现的移动次数，但不能直接解释为理论 k-kick 树中的交换成本。

## 8. Facility 层多 tier Cubby 与后台重建

### 8.1 tail 管理

`ensure_tail(table, facility)` 负责保证 Facility 有一个未满 tail：

```python
ensure_tail(t, f):
  if f.tail exists and f.tail.size < f.tail.capacity:
    return

  if f.tail_owned exists and f.tail_owned is full:
    将旧 tail 挂入 f.tiers[f.tail_tier]
    f.tail = null

  新建 tier=0 的 Cubby
  初始化 slots、occupied、MiniArray、FreeSlotTree
  f.tail = new cubby
  f.tail_owned = new cubby
```

实现效果：

- 插入优先进入 tail。
- tail 满后成为普通 tier 0 Cubby。
- 新 tail 继续承接后续插入波动。

### 8.2 Cubby 容量

`cubby_capacity(K, N, tier)`：

```python
x = N
repeat tier + 1 times:
  x = log2(max(2, x))
cap = K / (x * x)
cap = max(4, floor(cap))
cap = min(cap, K)
```

这是一种工程化容量公式，目标是让高 tier 与迭代对数规模发生关系。它不完全等同于设计文档中的理论 `s_j` 口径。

### 8.3 分布不变量与调度

`target_tier_count(j)` 计算目标数量：

```python
a = log^{(j)}(n)
b = log^{(j+1)}(n)
t_j = floor((a^2) / (b^2))
t_j 至少为 1
```

`maybe_schedule_rebuild(f)` 检查每个 tier：

```python
for j in 0 .. f.max_tier-1:
  tj = target_tier_count(j)
  cnt = f.tiers[j].size()

  if cnt > 3 * tj:
    enqueue rebuild_down(f, j)

  else if tj >= 2 and cnt < tj / 2:
    if tier j+1 exists and not empty:
      enqueue rebuild_up(f, j)
```

`run_rebuild_budget()` 每次执行一个调度任务：

```cpp
scheduler.step_budget(1)
```

### 8.4 合并：rebuild_down

代码注释中 `rebuild_down` 对应“tier j 多了，把多个 tier j 合并到 tier j+1”。流程：

```cpp
rebuild_down(f, j):
  tj = target_tier_count(j)
  从 f.tiers[j] 取出 tj 个 Cubby
  扫描这些 Cubby 中的占用槽
  recover_key_from_slot 恢复所有键

  newc = create_cubby(tier=j+1)
  for key in keys:
    free = cubby_find_free(newc)
    if no free: break
    写入 payload/meta
    更新 occupied、size、free_slots
    更新 Router: erase old, insert key -> (newc, pos)

  将 newc 放入 f.tiers[j+1]
  记录 rebuild_down 事件
```

### 8.5 拆分：rebuild_up

代码注释中 `rebuild_up` 对应“tier j 少了，从 tier j+1 拆出多个 tier j”。流程：

```python
rebuild_up(f, j):
  j2 = j + 1
  从 f.tiers[j2] 取出一个 big Cubby
  扫描 big 的占用槽并恢复所有键
  从 Router 中删除这些键的旧位置
  创建 tj 个 tier j Cubby

  for key in keys:
    target = news[i % news.size()]
    if target 有空槽:
      直接写入 payload/meta
      更新 Router
    else:
      调用 kkick_insert(target, key)

  将所有新 Cubby 放入 f.tiers[j]
  记录 rebuild_up 事件
```

### 8.6 与理论的差异

当前实现已经具备：

- `tiers` 多层 Cubby 容器。
- 唯一 tail Cubby。
- tail 满后挂回 tier。
- `t_j` 目标数量计算。
- `cnt > 3t_j` 与 `cnt < t_j/2` 的调度条件。
- tier 间合并与拆分函数。
- `RebuildScheduler` 调度队列。

替代与简化：

- `f.max_tier = params.k`，当前把 Facility tier 上限与 k-kick 参数绑定；理论上二者是两个不同层次。
- `RebuildScheduler::step_budget(1)` 每次执行一个完整任务，没有把一次 `O(|I|)` 重建拆成严格的逐步后台过程。
- `cubby_capacity` 是工程公式，不完全等同于理论 `s_j`。
- 删除路径会执行已有重建任务，但不会重新调用 `maybe_schedule_rebuild(f)` 检查是否因删除导致 tier 数量偏离。
- 合并时如果新 Cubby 空间不足会 `break`，这属于工程保护；理论结构需要更严密的容量与分布保证。

因此，当前实现可以说“实现了 Facility 层多 tier Cubby 的基础组织、重建调度和拆分/合并”，但不能说已经严格实现理论中的均摊后台重建证明。

## 9. 插入、查询、删除主流程

### 9.1 插入

入口：`HashTable::insert -> Impl::insert -> insert_no_lock`。

伪代码：

```python
insert(key):
  加锁
  r = insert_no_lock(key)

  if r.ok and r.inserted:
    maybe_resize_locked()
  if r.ok:
    run_rebuild_budget()
  run_migrate_budget()
  return r
```

`insert_no_lock`：

```python
insert_no_lock(key):
  if active is empty: fail

  gx = pi(key)
  f = facility_for_key(active, gx)
  b = route_bucket_for(active, gx)

  if active.D[b].locate(key) 命中:
    return inserted=false

  if old exists and old.D[b].locate(key) 命中:
    return inserted=false

  ensure_tail(active, f)
  kr = kkick_insert(f, f.tail, key, facility_index)
  if kr failed:
    return fail("tail_full")

  n_++
  记录 kick_count、router_probe_steps、cubby_tier
  记录 metrics
  maybe_schedule_rebuild(f)
  return inserted=true
```

实现语义：

- 插入具备幂等语义：已存在键返回 `inserted=false`。
- 迁移期间会同时检查 `active_` 和 `old_`，避免重复键。
- 新键只写入 `active_` 的 tail Cubby。

### 9.2 查询

入口：`HashTable::query`。

伪代码：

```python
query(key):
  gx = pi(key)
  f = facility_for_key(active, gx)
  b = route_bucket_for(active, gx)

  loc, steps = f.D[b].locate(key)
  found = loc exists and slot_pi_matches(loc.cubby, loc.slot, facility_r, active, gx)

  if not found and old exists:
    在 old 表重复同样流程

  记录 router_steps
  return found
```

查询的关键点是：Router 返回的只是候选槽位，最终必须用 `slot_pi_matches` 恢复并校验 `pi(key)`。这样可以避免 Router 记录与槽位状态不一致时产生错误命中。

### 9.3 删除

入口：`HashTable::erase`。

伪代码：

```python
erase(key):
  gx = pi(key)
  在 active 中定位 Router 桶和候选槽

  if active 命中:
    清空 slots[slot]
    meta.update(slot, empty)
    remove_occupied(slot)
    free_slots.mark_free(slot)
    Router.erase(key)
    n_--

    if 被删 Cubby 不是 tail:
      ensure_tail()
      if tail 有元素:
        从 tail 取最后一个 occupied 槽
        recover_key_from_slot 得到 moved_key
        清空 tail 中该槽
        将 moved_key 写入刚删出的槽位
        更新 occupied、free_slots、Router
        moved_total++

  if old exists:
    在 old 中也尝试删除 key

  记录 delete metrics
  maybe_resize_locked()
  run_rebuild_budget()
  run_migrate_budget()
```

删除的结构目标是：非 tail Cubby 删除后，从 tail 搬一个元素回填，使非 tail Cubby 尽量保持满载。

## 10. 扩缩容与双表渐进迁移

### 10.1 触发条件

`maybe_resize_locked`：

```python
if no old_ and n_ > active.N * load_factor:
  start_migration(2 * active.N)

else if no old_ and n_ > 0 and n_ < active.N / 4:
  start_migration(max(2, active.N / 2))
```

### 10.2 启动迁移

`start_migration(newN)`：

```python
scheduler.clear()
记录 resize_start
old_ = move(active_)
active_.reset()
migrate_progress_ = 0

active.N = next_pow2(newN)
active.K = choose_K(active.N)
创建 active.facilities
for each facility:
  清空 tiers
  max_tier = params.k
  D = K 个 Router
  ensure_tail()
```

### 10.3 每次迁移一个 Facility

`run_migrate_budget()`：

```python
if old_ 不存在:
  return

if migrate_progress_ >= old.facilities.size:
  old_.reset()
  migrate_progress_ = 0
  记录 resize_finish
  return

of = old.facilities[migrate_progress_]
keys = []

扫描 of.tail_owned 中所有占用槽:
  recover_key_from_slot -> keys

扫描 of.tiers 中所有 Cubby 的占用槽:
  recover_key_from_slot -> keys

for key in keys:
  gx = pi(key)
  nf = facility_for_key(active, gx)
  nb = route_bucket_for(active, gx)
  if active Router 已存在 key:
    continue
  ensure_tail(active, nf)
  kkick_insert(nf, nf.tail, key, new_facility_index)

清空旧 Facility 的 Router、tiers、tail
migrate_progress_++
```

迁移不直接复制旧 `payload` 和 `MetaEntry`，而是恢复原始键后按新表的 `N`、`K`、Facility 编号和 Cubby 容量重新写入。这一点是必要的，因为商压缩元数据依赖表上下文。

## 11. 观测、指标与持久化

### 11.1 进程内 Metrics

`include/metrics.h` 和 `src/metrics.cpp` 实现进程级指标：

- 操作次数：`ops_init`、`ops_insert`、`ops_query`、`ops_delete`。
- 移动次数：`insert_moved_total/max`、`delete_moved_total/max`。
- Router 步数：`router_steps_total/max`。
- 元数据逻辑位数：`meta_bits_total/max`。
- 操作延迟直方图：初始化、插入、查询、删除。
- 事件计数：`rebuild_down`、`rebuild_up`、`resize_start`、`resize_finish`。

这些指标用于解释当前实现行为，不等同于理论证明中的严格空间或时间界。

### 11.2 结构快照

`HashTable::visit_structure` 遍历 active 表中的所有 Facility、tier Cubby 和 tail Cubby，构造 `CubbyStructureView`：

```cpp
struct CubbyStructureView {
  int facility_id;
  int tier;
  int capacity;
  int size;
  bool is_tail;
  vector<optional<uint64_t>> slot_keys;
};
```

遍历时会通过 `recover_key_from_slot` 恢复每个占用槽中的原始键。该接口用于生成：

- Facility 信息。
- Cubby tier、capacity、size、is_tail。
- Slot 是否占用及 key hash。
- tier 统计。

### 11.3 MySQL 分析表

`src/storage_mysql.cpp` 中的分析相关接口包括：

- `analytics_create_snapshot(...)`
- `analytics_enqueue_metric(...)`
- `analytics_flush_metrics(...)`
- `analytics_replace_structure(...)`
- `analytics_summary_json(...)`
- `append_test_session_log(...)`

相关表包括：

- `snapshot_meta`
- `facility`
- `cubby`
- `slot_snapshot`
- `tier_stat`
- `otsh_op_metrics`
- `otsh_test_session_log`

其中 `otsh_op_metrics` 保存 `operation_type`、`probe_count`、`kick_count`、`latency_ns`、`cubby_tier`。结构表保存 Facility/Cubby/Slot/tier 分布，用于实验后分析和可视化。

## 12. 测试与微基准

### 12.1 功能测试

`src/test_runner.cpp` 覆盖：

- 初始化成功。
- 初始化后状态有效。
- 批量插入唯一键。
- 查询全部已插入键。
- 删除一半键。
- 查询已删除键应未命中。
- 对剩余键重复插入，应返回 `inserted=false`。

测试结束输出单行 JSON，并尝试写入 MySQL；如果 MySQL 不可用，则写入本地日志。

### 12.2 性能基准

`perf/bench.cpp` 测量内存态哈希表路径，不经过 HTTP 和 MySQL：

- 初始化耗时。
- 插入延迟。
- 顺序命中查询延迟。
- 随机命中查询延迟。
- 删除一半键延迟。
- 混合查询延迟。
- 全局 Metrics 快照。

基准程序支持参数：

```python
otsh_perf [n_insert] [n_rand_queries] [table_n_hint]
```

并通过 `OTSH_SEED` 固定随机种子。

## 13. 理论结构与当前实现对照

### 13.1 已落地实现的部分

1. 可逆置换

   - 理论要求：用随机置换使地址拆分和键恢复具备一致上下文。
   - 当前实现：3 轮 Feistel，提供 `pi` 和 `inverse`。
2. Facility--Cubby--Slot 三层结构

   - 理论要求：Facility 管理局部范围，Cubby 保存局部元素，Slot 是具体槽位。
   - 当前实现：`Facility`、`Cubby`、`slots` 三层完整存在。
3. 局部 Router

   - 理论要求：Local Query Router 负责局部键到位置的映射。
   - 当前实现：Patricia 风格二叉前缀树，保存 `key -> (Cubby*, slot)`。
4. MiniArray

   - 理论要求：变长元数据紧凑存储。
   - 当前实现：按条目 bitlen 维护 offsets，并重打包到连续 bit buffer。
5. 商压缩

   - 理论要求：槽位只保存无法由位置恢复的剩余位。
   - 当前实现：`slots[i]` 保存 `pi(x) >> logN`，`MetaEntry` 保存 `delta`、`mid_bits` 等恢复信息。
6. 单 Cubby 内 k-kick 插入

   - 理论要求：在 Cubby 内进行受控插入和驱逐。
   - 当前实现：tail Cubby 内有界随机探针和踢出链。
7. Facility 多 tier 管理

   - 理论要求：按 tier 维护不同尺寸 Cubby，并通过拆分/合并维持分布。
   - 当前实现：有 `tiers`、`target_tier_count`、`maybe_schedule_rebuild`、`rebuild_down`、`rebuild_up`。
8. tail 机制

   - 理论要求：只允许少量局部波动集中在 tail。
   - 当前实现：唯一 `tail_owned`；满 tail 挂回 tier；删除非 tail 后从 tail 回填。
9. 双表渐进迁移

   - 理论要求：扩缩容不能一次性阻塞全部元素搬迁。
   - 当前实现：`active_`/`old_` 双表，每次更新后迁移一个旧 Facility。

### 13.2 未完整实现及替代方式

1. 完整 k-kick 树

   - 未实现内容：深度优先级、饱和状态、严格合法驱逐对象、理论探针序列。
   - 替代方式：在单个 tail Cubby 内使用 `probe_slot` 生成伪随机探针，执行最多 `k` 次踢出，失败后用 `FreeSlotTree` 兜底找空槽。
2. 理论 MiniArray

   - 未实现内容：内存 B 树、查表法、均摊常数时间更新。
   - 替代方式：保留变长编码和连续 bit buffer，但每次更新后整体重打包。
3. 理论 Local Query Router

   - 未实现内容：真实紧凑 bitstream 上的常数时间局部更新。
   - 替代方式：用 Patricia 风格二叉前缀树保存结构，插入/删除后整体重建，并用 `BitWriter` 估算编码位数。
4. 严格 Facility 后台重建

   - 未实现内容：一次 `O(|I|)` 重建细分为严格逐步后台过程，完整均摊证明所需的不变量维护。
   - 替代方式：使用 `RebuildScheduler` 排队，每次操作执行一个完整重建任务。
5. 理论 `s_j` 容量公式

   - 未实现内容：完全按设计文档公式确定每个 tier 的 Cubby 尺寸。
   - 替代方式：`cubby_capacity` 使用 `K / (iter_log(N, tier+1)^2)` 并限制在 `[4, K]`。
6. 真实内存空间统计

   - 未实现内容：精确统计 `vector`、对象头、指针、optional 等运行时开销。
   - 替代方式：统计 `MiniArray::bits_total()` 和 `Router::bits_total()` 作为逻辑编码位数。
7. 删除触发分布重建

   - 未实现内容：删除后重新检查 tier 分布不变量。
   - 替代方式：删除后执行已有调度任务和迁移预算，但不主动调用 `maybe_schedule_rebuild`。
8. 迁移中的后台重建协同

   - 未实现内容：理论层面重建、扩缩容、分布维护之间更细粒度的调度协调。
   - 替代方式：扩缩容开始时清空重建调度队列，迁移期间按 Facility 逐步恢复键并重新插入。

## 14. 当前实现的语义边界

当前代码可以支持：

- 插入、查询、删除、重复插入。
- active/old 迁移窗口内的查询和删除。
- 商压缩后的键恢复与校验。
- Router 路由步数统计。
- 插入/删除移动次数统计。
- MiniArray 和 Router 逻辑编码位数统计。
- Facility/Cubby/Slot 结构快照。
- MySQL 分析表写入与后续查询。

需要谨慎解释的地方：

- `kick_count` 是工程踢出链移动次数，不是完整理论 k-kick 树交换成本。
- `meta_bits_total` 是逻辑统计，不是实际内存占用。
- `Router::bits_total()` 是模拟编码位数，不是实际序列化存储。
- `rebuild_down/rebuild_up` 说明代码实现了 tier 合并/拆分机制，但不能直接推出理论均摊复杂度。
- 性能测试只说明当前参数、当前实现和当前运行环境下的行为，不是通用哈希表性能结论。

## 15. 总结

系统已经将论文中的主要工程结构落到了代码中：可逆置换负责地址拆分和键恢复，Facility--Cubby--Slot 承载局部存储，Router 负责局部定位，MiniArray 保存变长元数据，商压缩减少槽位中直接保存的信息，tail Cubby 承接更新波动，工程版 k-kick 在单 Cubby 内执行有限踢出，多 tier Cubby 与重建调度提供 Facility 层的分布维护接口，active/old 双表完成渐进扩缩容。

与理论相比，当前实现的重点是正确运行、结构可观测和实验可解释。对于理论中复杂的常数时间动态编码、严格 k-kick 树、细粒度均摊后台重建和真实空间上界证明，代码采用了更直接的工程替代方式。这些替代方式降低了实现复杂度，也使实验结果必须按“当前实现口径”解释。
