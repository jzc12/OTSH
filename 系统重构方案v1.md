

## 一、系统核心设定

### 1.1 全域与哈希基础

- 全域 U 大小：∣U∣=poly(n)

- N：哈希表当前容量所在 2 的幂区间 [N,2N]

- K = polylog n：每个 Facility 的基准大小

- $g^*(x)$：全局路由哈希 = x 经过随机置换 π 后的前 logN 位

    ```
    π(x):  [ ...更高位... ]  [ g*(x) ]
                      ↗        ↖
                （左：高位）    （右：低位）
                
    g*(x):  [ Facility位 ]  [ Cubby位 ]  [ g_I(x)：local query router 位 ]
             （最高位段）   （中间位段）    （最低位段）   
    ```

- storage[i] = $ \pi(x)[logN + 1, logU] $

- $g^*(x) = \pi(x)[1, log N] $

- Facility 编号: $r = g^*(x)[logK+1, logN]$  

-  $g_I(x) = g^*(x)[1, log|I|]$， （其中 I 为cubby 的容量大小）

### 1.2 核心组件层级关系

整个系统存储看作一个数组，均等划分为多个 Facility；每个 Facility 管理不同大小的多层级 cubby；每个 cubby 内部包含存储数组、k-kick 插入逻辑及三组 mini-array（A/M/B）。



## 二、Facility 设计

### 2.1 基础属性

- Facility 大小 K=polylog N
- 内部包含一个基于 B 树实现的 mini-array，用于高效管理数据访问、插入、删除操作。

### 2.2 mini-array 详细设计

```text
MiniArray
    ├── O(1) 深度树
    ├── Packed Leaf
    ├── Bitmap
    ├── Rank / Select
    └── Lookup Table
```

#### （1）基础常量与类型定义

```cpp
template<typename T>
class MiniArray
{
private:
    struct Leaf {
        Bitmap bitmap;       // 标记哪些位置有有效元素
        PackedArray<T> data; // 紧凑无间隙存储元素
    };
    struct Internal {
        uint16_t subtree_size[F];  // 每个子树的元素总数（用于Rank/Select）
        Node* children[F];         // 子节点指针（仅 Θ(loglogn) 位）
    };
    Node* root;

public:
    T Access(uint32_t idx);
    void Insert(uint32_t idx,T val);
    void Delete(uint32_t idx);
    void Update(uint32_t idx,T val);
};
```

#### （1）Access

- 从根节点向下遍历
- 用  subtree_size 直接定位目标叶子
- 在叶子的 Bitmap 中找到真实偏移
- 从 PackedArray 读取数据

```text
函数 Access(idx):
    当前节点 = root
    当前剩余索引 = idx

    循环 O(1) 层（直到到达叶子）：
        找到子树 i，满足 sum(subtree_size[0..i-1]) ≤ 当前剩余索引 < sum(subtree_size[0..i])
        当前剩余索引 = 当前剩余索引 - sum(subtree_size[0..i-1])
        当前节点 = 当前节点.child[i]

    // 当前节点已是叶子
    pos = Select(当前剩余索引)  // 用 bitmap 找到真实存储位置
    返回 当前节点.data[pos]
```

#### （2）Insert

- 定位目标叶子
- 更新 Bitmap + 写入数据
- 若叶子容量超限 → 分裂节点
- 向上递归更新所有 subtree_size

```text
函数 Insert(idx, val):
    路径栈 = 空栈
    当前节点 = root
    当前剩余索引 = idx

    循环 O(1) 层：
        路径栈.push(当前节点)
        找到子树 i，满足 sum(subtree_size[0..i-1]) ≤ 当前剩余索引 < sum(subtree_size[0..i])
        当前剩余索引 -= sum(subtree_size[0..i-1])
        当前节点 = 当前节点.child[i]

    // 写入叶子
    leaf = 当前节点
    pos = Select(当前剩余索引)
    leaf.bitmap.set(pos)
    leaf.data.insert(pos, val)

    // 叶子满则分裂
    如果 leaf.size ≥ MAX_LEAF_SIZE:
        Split(leaf)

    // 自底向上更新 subtree_size
    对 路径栈 中每个内部节点 node：
        node.subtree_size[i] += 1
```

#### （3）Delete

- 定位目标叶子
- 清空对应 Bitmap 位 + 移除数据
- 若节点过小 → 合并相邻节点
- 向上更新 subtree_size

```text
函数 Delete(idx):
    路径栈 = 空栈
    当前节点 = root
    当前剩余索引 = idx

    循环 O(1) 层：
        路径栈.push(当前节点)
        找到子树 i，满足 sum(subtree_size[0..i-1]) ≤ 当前剩余索引 < sum(subtree_size[0..i])
        当前剩余索引 -= sum(subtree_size[0..i-1])
        当前节点 = 当前节点.child[i]

    // 删除叶子元素
    leaf = 当前节点
    pos = Select(当前剩余索引)
    leaf.bitmap.unset(pos)
    leaf.data.remove(pos)

    // 元素太少则合并
    如果 leaf.size 太小：
        Merge(leaf)

    // 自底向上更新大小
    对 路径栈 中每个内部节点 node：
        node.subtree_size[i] -= 1
```

#### （4）Update

- 直接覆盖对应位置数据

```text
函数 Update(idx, val):
    当前节点 = root
    当前剩余索引 = idx

    循环 O(1) 层：
        找到子树 i，满足 sum(subtree_size[0..i-1]) ≤ 当前剩余索引 < sum(subtree_size[0..i])
        当前剩余索引 -= sum(subtree_size[0..i-1])
        当前节点 = 当前节点.child[i]

    leaf = 当前节点
    pos = Select(当前剩余索引)
    leaf.data[pos] = val
```

#### （5）Split (leaf) 伪代码（叶子分裂）

```text
函数 Split(leaf):
    新叶子 = 新建 LeafNode
    中间位置 = leaf.size / 2

    // 搬运一半数据
    新叶子.bitmap = leaf.bitmap 后半段
    新叶子.data = leaf.data 后半段
    leaf 保留前半段

    // 找到父节点，插入新子节点
    parent = leaf 的父节点
    在 parent.child 中插入 新叶子
    parent.subtree_size 对应位置分裂更新
```

#### （6）Merge (leaf) 伪代码（叶子合并）

```text
函数 Merge(leaf):
    兄弟节点 = 相邻叶子
    parent = leaf 的父节点

    // 把兄弟数据合并到 leaf
    leaf.bitmap |= 兄弟节点.bitmap
    leaf.data.append(兄弟节点.data)

    // 父节点删除兄弟
    从 parent.child 移除兄弟节点
    更新 parent.subtree_size
```

#### （7）Rank / Select 伪代码（论文必备）

```text
函数 Rank(bitmap, i):
    // 前 i 位 1 的个数
    返回 LookupTable[bitmap 高段] + LookupTable[bitmap 低段]

函数 Select(bitmap, k):
    // 第 k 个 1 的位置
    返回 LookupTable[k] 直接查表得到位置
```



## 三、多层级 Cubby 设计

### 3.1 层级属性

Facility 管理不同大小的 cubby，包含不同层级的 tiered cubbies：

- j-tiered cubby 数量： $t_i = \Theta(\frac{(log^{(j+1)}n)^2}{(log^{(j)}n)^2})$
- j-tiered cubby 大小：$r_j = \frac{K}{(log^{(j)}n)^2}$ 
- 初始状态：仅包含 1 个 1-tiered cubby（作为 tail cubby）

### 3.2 动态调整规则

基于 j-tiered cubbies 数量触发合并 / 拆分：

1. 数量 $\geq 3t_i$ ：将 $t_i$ 个 j-tiered cubbies 合成一个 ${(j+1)}$-tiered cubby
2. 数量 $\leq \frac{t_i}{2}$ ： 将一个 ${(j+1)}$-tiered cubby（如果有的话），拆成 $t_i$ 个 j-tiered cubbies 
3. 合并 / 拆分时，需取出 cubby 内所有元素重新插入

### 3.3 Cubby 内部组件

每个大小为 I 的 cubby 包含：

- 存储数组 storage：存储真正的 key/value，大小为 I
- k-kick 插入逻辑：动态管理元素存储位置
- mini-array A：存储 I 个 local query router（I=cubby 容量）
- mini-array M：实现常数时间插入 / 删除的额外元数据（存储各层级 bin 的空闲槽位图）
- mini-array B：  ( 当storage[i] 存储 x 对应的信息时， B[i]存储 delta  )
    - delta $ = g_I(x) - i$ 
    - $g^*(x)[log|I|+ 1, log K]$
    - 配合Facility 编号: $r = g^*(x)[logK+1, logN]$   可以完整还原出 $g^*(x)[1, logN]$

### 3.4 操作

- 每次插入时都会插入tail cubby，当 tail cubby 满了的话，会新建一个tail cubby，并把旧的 tail cubby 视作一个 1-tiered cubby ，可能会触发合并。
- 每次删除，如果删除的元素不在tail cubby 中，会随机从tail cubby中取一个元素回填到对应的cubby中。如果tail cubby空了，就会从 1-tiered cubby 中取一个出来，重新当作 tail cubby，这期间可能会触发拆分。



## 四、k-kick 算法设计

> 以下用到的 x ，并不是原始 x ，而是 $\pi(x)$ 

### 4.1 层级 bin 定义

k-kick 为逻辑分层，无物理存储结构：

- 深度 0：整个 cubby 的 大小 $s_0$ = K =  polylog n， 只有一个 bin
- 深度 1：bin 大小 $s_1 = \Theta((log K)^6) $， 把第 0 层那个大 bin，均匀切成许多大小 $s_1$ 的子 bin
- ...
- 深度 i：bin 的大小为 $s_i = \Theta((log^{(i)}K)^6)$ 

### 4.2 核心序列定义

#### （1）偏好序列

 $g_0(x) \to g_1(x) \to \dots g_k(x) $， 这里我们假定 k-kick 有 k 层：

- $g_0(x)$ 是深度0的 bin ，即整个cubby本身
- $g_1(x)$ 是深度0的 bin 均分成深度1 的 多个 bins 其中的一个
- $g_2(x)$  是 $g_1(x)$  选中的那一个 bin 继续切分的多个深度2大小 bins 其中的一个
- ……
- $g_k(x)$  是沿着上述路径落到最底层的最小的 bin 叶子

- 其中 $g_{i+1}(x)$  一定是 $g_{i}(x)$  切分出来的子 bin， 每一个 x 随机选一个叶子 bin ，往上回溯，可以得到其所有的祖先 $g_0(x), g_1(x), \dots, g_{k-1}(x) $ 。

#### （2）探测位置序列

$h_i(x)$ 是 x 在cubby 存储数组里的第 $i$ 个候选位置，按照先深后浅的顺序，遍历$g_k(x), g_{k-1}(x), \dots, g_0(x) $，，依次遍历 bin 内所有槽位：

- 深度 $i$ 的bin ，对 $\forall j \in [0, s_i - 1] $ ， $h_{(k+1 - i)*s_i + j}(x)$ 是 $g_i(x)$ 这个 bin 的第 $j+1$ 个槽位

计算过程：前面所有比 i 更深的层一共占据的槽位数为 $s_k + s_{k-1} + \dots + s_{i+1} $

### 4.3 bin 饱和定义

bin 处于满状态，且其中所有元素的深度 $\geq$ 当前 bin 的深度。

### 4.4 插入流程

1. 深度选择：定义一个随机哈希函数 $s(x) \in [0, k] $ ，插入深度depth = $\min{(\text{最大可用深度}, s(x))}$（最大可用深度为当前深度最大且不饱和的 bin 深度）
2. 元素插入：在 $g_{depth}(x)$ bin 中，有空闲槽位则直接放入；无空闲则踢走深度最小的元素
3. 踢除递归：被踢元素向上层 bin 找空位，最多触发 k 次 kick



```text
// ==============================
// 常量与定义
// ==============================
K = cubby 大小 = polylog n
k = k‑kick 最大深度（常数，如 4）
// 深度 i 的 bin 大小（严格按论文）
s[i] = Θ( (log^(i) K)^6 )，i = 0,1,...,k

depth_max = 最大可用深度（当前最深的不饱和 bin）

// 元素 x 已经是 π(x)
// ==============================
// 4.1 层级 bin 定义（逻辑分层，无物理存储）
// ==============================
Bin(x, i):
    返回 x 在深度 i 所属的 bin（区间）
    深度 0：唯一 bin = 整个 cubby [0, K‑1]
    深度 1：均匀切分为大小 s[1] 的 bin
    ...
    深度 i：均匀切分为大小 s[i] 的 bin

// ==============================
// 4.2 偏好序列 g_0(x) → g_1(x) → ... → g_k(x)
// ==============================
// 自顶向下：root → 叶子
PreferenceSequence(x):
    g[0] = Bin(x, 0)
    for i = 1 to k:
        g[i] = g[i‑1] 中均匀切分后、x 所属的子 bin
    return g[0...k]

// ==============================
// 4.2 探测位置序列 h_j(x)（先深后浅）
// ==============================
// 遍历顺序：g_k → g_{k‑1} → ... → g_0
// 每个 bin 内遍历所有槽位
ProbeSequence(x):
    g = PreferenceSequence(x)
    seq = empty list
    for i from k down to 0:
        bin = g[i]
        for pos in bin.start to bin.end:
            seq.append(pos)
    return seq  // h_1(x), h_2(x), ... 依次是 seq[0], seq[1], ...

// ==============================
// 4.3 bin 饱和判断（核心定义）
// ==============================
IsSaturated(bin, depth_d):
    // bin 满 + 所有元素深度 ≥ depth_d
    if bin 未满:
        return false
    for each element y in bin:
        if y.insert_depth < depth_d:
            return false
    return true

// 找到当前最大可用深度
GetMaxUsableDepth(x):
    g = PreferenceSequence(x)
    for d from k down to 0:
        if not IsSaturated(g[d], d):
            return d
    return 0

// ==============================
// 4.4 随机深度选择
// ==============================
s(x) = 随机哈希值 ∈ [0, k]
depth = min( GetMaxUsableDepth(x), s(x) )

// ==============================
// 4.4 插入 + kick 递归（核心流程）
// ==============================
function InsertKick(x):
    // 1. 确定插入深度
    depth_max_usable = GetMaxUsableDepth(x)
    s_x = random(0, k)
    depth = min(depth_max_usable, s_x)

    // 2. 定位目标 bin
    g = PreferenceSequence(x)
    target_bin = g[depth]

    // 3. 尝试放入 target_bin
    if target_bin 有空闲槽位:
        pos = 任意空闲位置
        store x at pos
        x.insert_depth = depth
        return OK
    else:
        // 4. 踢走深度最小的元素 y
        y = target_bin 中 insert_depth 最小的元素
        evict_pos = position of y
        // 把 x 放进去
        remove y from evict_pos
        store x at evict_pos
        x.insert_depth = depth

        // 5. 递归把 y 向上层重新插入
        ReInsert(y)

// 被踢元素向上找空位
function ReInsert(y):
    for d from y.insert_depth - 1 down to 0:
        bin = Bin(y, d)
        if IsSaturated(bin, d) == false and bin 有空闲:
            pos = 空闲位置
            store y at pos
            y.insert_depth = d
            return
    // 最多递归 k 次，论文保证一定能放下
```



## 五、Local Query Router 设计

### 5.1 核心目标

有了上述的上述的偏好序列和探测位置序列，我们还需要考虑如何让查询做到 $O(1)$ 的时间复杂度，不遍历整个探测序列 $h_1(x), h_2(x), \dots $ ， 因为k‑kick 插入会kick 元素，元素实际位置 $ \ne $ 首选位置 $g_k(x)$，也不等于 $h_1(x)$ ，插入用 k‑kick 动态挪动了位置，查询如果再按 $h_1(x), h_2(x), \dots $ 扫过去，就不是 $O(1)$ 的时间复杂度，所以必须存一张精准映射：key → 它在 cubby 数组里的真实位置。

### 5.2 映射规则

- 定义$ g_I(x) = g(x) \in [0, I - 1] $ （与偏好序列无关）：cubby 内部首选槽位号 (我们假设cubby 容量为 I )

- 所有$g(x) = i$ 的 key  由第 $ i$ 号 local query router 管理（即 A [i]）

- 找到对应 local query router 后，查询 x 得到探测位置序列 $j$ , 去 $h_j(x)$ 这个位置取元素。

    

### 5.3 存储结构（二叉前缀树）

对外暴露接口（key 为 π(x)）：

```text
insert(key, uint j)      // j ≤ log K，只需 O(loglog n) 位
delete(key)
query(key) → j 或 不存在 	// 不存完整 key，仅存区分 key 的最短前缀
```

#### 插入实现

```text
function insert(key, j):
    current = root
    for bit in key:
        if bit == 0:
            if current.child0 == null:
                current.child0 = new Node()
            current = current.child0
        else:
            if current.child1 == null:
                current.child1 = new Node()
            current = current.child1
    // 到达叶子
    current.is_leaf = true
    current.j_value = j
```

#### 查询实现

```text
function query(key):
    current = root
    for bit in key:
        if bit == 0:
            if current.child0 == null: return NOT_FOUND
            current = current.child0
        else:
            if current.child1 == null: return NOT_FOUND
            current = current.child1
    if current.is_leaf:
        return current.j_value
    else:
        return NOT_FOUND
```

#### 删除实现

```text
function delete(key):
    走一遍路径，找到叶子，标记非叶子即可
```

## 系统架构

```text
HashTable
    ├─ Facility #r（r = g*(x)[logK+1, logN]）
    │   ├─ 元数据 Mini-array D			（定位元素所属 Cubby）
    │   │   ├─ O(1) 深度 B 树结构
    │   │   ├─ Packed Leaf 紧凑存储
    │   │   ├─ Bitmap 有效位标记
    │   │   ├─ Rank / Select 快速寻址
    │   │   └─ Lookup Table
    │   │
    │   └─  Cubbies
    │       ├─ storage[I]			  （键值紧凑存储）
    │       ├─ Mini-array A[I]		  （Local Query Router 路由表）
    │       │   └─ 二叉前缀树：key → 探测索引 j
    │       ├─ Mini-array B[I]		  （商化还原元数据）
    │       │   ├─ Δ = g_I(x) − pos
    │       │   └─ g*(x) 中间位片段	   （用于完整还原 g*(x)）
    │       └─ Mini-array M[I]         （空闲槽/层级饱和状态）
    │
    ├── ...
    └── Facility
```

```text
Facility
├─ Tail Cubby（唯一未满）
├─ Tier 1: Cubby、Cubby、Cubby
├─ Tier 2: Cubby、Cubby
└─ Tier 3: Cubby
    ↓
Cubby
├─ storage[I]			  （键值紧凑存储）
├─ Mini-array A[I]		  （Local Query Router 路由表）
├─ Mini-array B[I]		  （商化还原元数据）
├─ Mini-array M[I]         （空闲槽/层级饱和状态）
└─ k-kick 逻辑
```

