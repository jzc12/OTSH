## 一、项目概述

### 1.1 目标

复现论文 On the Optimal Time/Space Tradeoff for Hash Tables 中提出的哈希表结构，在工程上实现其核心性质与操作接口。

- 查询：在“区间扫描次数”意义下，单表内每次查询两个连续下标区间（偏好 mini-bin 与同一 bin 的 fallback），与“常数次探测”一致；
- 插入：有界深度 k-kick，深度超过 k 时尝试将键仅放入 fallback 兜底路径。
- 空间：由目标规模 n、load_factor 与派生的 bin 尺寸共同决定；每键在库中存完整键值与 16 位指纹。
- 功能：插入、查询、删除、在线扩容、HTTP 与前端实验接口。

### 1.2 核心思想

结构可概括为：全局分桶（bin）与桶内分 mini-bin，加固定大小 fallback 区域，再用有界深度置换（k-kick）解决冲突。

- 全局划分为若干 bin；每个 bin 内划分为多个 mini-bin，尾部为 fallback 槽。
- 每个 key 经哈希属于唯一 bin，再经独立哈希得到偏好 mini-bin；
- k-kick 将冲突元素在有限步内重排；本实现中踢出槽为确定性函数，避免数据库端高代价随机。
- 查询在逻辑上只检查“偏好 mini-bin 对应下标范围”和“该 bin 的 fallback 下标范围”。

### 1.3 本项目的实现方式

| 项目 | 说明 |
| --- | --- |
| 存储与计算 | MySQL ，槽位与元数据、事务、行锁均落库 |
| 服务进程 | C++20，可执行文件 otsh_backend，HTTP 由 cpp-httplib 提供，JSON 由 nlohmann_json 解析 |
| 连接库 | 官方 MySQL客户端，需 mysql.h 与对应库文件 |
| 前端 | Vue 3、TypeScript、Vite，通过 /api 调用后端 |

## 二、整体结构设计

### 2.1 全局结构

```text
HashTable
├─ 元信息（n, k, load_factor, 三枚种子, total_bins, 扩展开关与进度等）
└─ 共 B 个 Bin
   ├─ 若干 MiniBin 槽位（主存，连续下标）
   └─ Fallback 区（每 bin 固定槽数，本实现为 8）
```

### 2.2 参数定义（理论目标与本实现对应关系）

| 参数 | 理论 | 本实现中的说明 |
| --- | --- | --- |
| n | 数据规模 10^4 量级起 | 初始化时由用户指定；接口校验约 10 到 5×10^6 |
| k | 1 或 2、3、4 等，控制重排深度上界 | 由用户指定；接口允许 0 到 8，k=0 即不允许加深递归 |
| mini_bin_size, num_mini_bins | 同阶，约 O(log^(k) n) | 由函数 ilog_iter(n, max(1,k)) 派生，再限制在 4 到 64 之间，避免过小无结构或过大查询区间 |
| fallback_size | 常数 | 固定 8 |
| bin_size | mini 区加 fallback 区 | mini_bin_size × num_mini_bins + fallback_size |
| total_bins | 与容量和 bin_size 相关 | 由目标容量与 bin_size 上取整得到，至少 1，在线扩容时可翻倍并持久化 |
| 指纹 | 16 位 | fp=0 表示空槽，非空键的指纹经哈希后恒不为 0，与建表列类型一致 |
| load_factor | 高负载 0.95 到 0.99 区间常见 | 用于由 n 推导目标槽位数，实际负载由已用行数与 capacity_slots 相除得到 |

由 n、k、load_factor 派生尺寸时，

本实现中核心关系为：先算单 bin 尺寸 bin_size，再算目标总槽位约 n/load_factor 的上取整，再算 total_bins 与 capacity_slots，细节与代码中 derive_sizes 一致。

### 2.3 映射关系

对任意 key 与三枚种子 seed1、seed2、seed3（与代码中 HashFunc 一致）：

```text
b = h_bin(key)   由 splitmix64(key 与 seed1 异或) 对 total_bins 取模
m = h_pref(key)  由 splitmix64(key 与 seed2 异或) 对 num_mini_bins 取模
```

在单个 bin 内，全局连续下标 idx 的构造方式为：

```text
bin 起点 offset_bin = b * bin_size
mini-bin 对应 idx 段：offset_bin + m*mini_bin_size 起，连续 mini_bin_size 个
fallback 对应 idx 段：接在全部 mini-bin 之后，连续 fallback_size 个
```

------

## 三、核心数据表示

### 3.0 逻辑与存储

```text
逻辑上：SLOT[0 .. total_bins*bin_size - 1] 一维数组

实现上：表 hash_table 每行一条槽位，主键 idx 即数组下标，不要求进程内开整块内存
```

### 3.1 槽位行（与当前实现一致）

本实现不单独使用 is_used 列；是否为空由 key 与 fp 共同表示：

| 列名 | 含义 |
| --- | --- |
| idx | 主键，全局下标 |
| key | 空表示槽空；有值时表示存储的 64 位无符号键，且在该列上有唯一约束 |
| fp | 0 表示空；非 0 为 16 位指纹 |

### 3.2 元数据

除理论参数 n、k、load_factor 与三枚种子外，在线扩容在表 ht_meta 中至少包含：

当前 total_bins、is_resizing、new_total_bins、resize_progress 等，与代码中 load_or_init_meta 及 resize 流程一致。

### 3.3 哈希子程序

- h_bin、h_pref 均基于 splitmix64 的 64 位混洗，再对总桶数或 mini-bin 数取模。
- fingerprint 由第三枚种子经 splitmix64 取低 16 位，若为 0 则改为 1，以便 fp=0 专用于空槽。
- 在满 mini-bin 中选取踢出位置时，使用与指纹、深度、种子有关的确定性函数，再对 mini_bin_size 取模，与代码中 kick_slot 一致，而非在 SQL 中随机取行。

------

## 四、核心算法

### 4.1 插入

顶层插入：

```
Insert(x):
    1. 将 x 加入 fallback（special slot）
    2. success = place(x, 0)
    3. if success:
           从 fallback 移除 x
    4. else:
           留在 fallback
```

核心递归过程：

```text
place(x, depth):
    if depth > k:
        return FAIL

    b = h_bin(key)
    m = h_pref(key)
    求出该偏好 mini-bin 在全局 idx 上的一段闭区间

    if 有空闲:
        插入 x
        return SUCCESS
    else:
        随机选出 y
        将 x 放入 y 的位置
        将 y 加入 fallback 
        success = place(y, depth + 1)
        if success:
            从 fallback 移除 y
        return SUCCESS
```

### 4.2 查询

```text
1. 计算 b = h_bin(x)
2. 计算 m = h_pref(x)

3. 计算区间：
   start = b * bin_size + m * mini_bin_size
   end   = start + mini_bin_size - 1
   与当前 bin 的 fallback 段在 idx 上的范围

4. 查询 mini-bin：
   做一次区间查询，探针计数加 1
   若存在行 key 与指纹与查询一致，返回找到

5. 若未找到：
   在 fallback 段做一次区间查询，探针计数再加 1
   若存在行一致，返回找到

6. 返回未找到
```

### 4.3 删除

```text
b = h_bin(x)
m = h_pref(x)
在 mini-bin m 中查找，找到则删除
否则在 fallback 中查找，找到则删除
```

------

## 五、理论解释



## 六、复杂度

| 操作 | 渐近与工程含义 |
| --- | --- |
| 查询 | 单表上通常 1 到 2 次区间级操作；两表时最多约 4 次 |
| 插入、删除 | 与 k 及 k-kick 路径、事务与行锁相关；可用接口统计平均探针、耗时 |
| 空间 | 与 capacity_slots 及行宽相关 |

------

## 七、失败与重建

论文意义下低概率失败可通过换种重建整表；本项目中：

- 全表与元数据多由 /api/init 在可控条件下重建，用于实验冷启动，不等同于运行时自动重建的同一实现路径。
- 单次插入在极端下可能仅落入 fallback 或直接失败，错误信息或备注由返回 JSON 描述。

------

## 八、动态扩容

与内存中双指针表不同，实现采用两张物理表与元数据，大致如下。

### 8.1 触发

> 在插入路径上检查当前负载
> 若未处于扩容中，且 已用槽 / capacity_slots 不小于 0.99
> 在 ht_meta 中置 is_resizing、new_total_bins=2*原 total_bins、resize_progress=0

### 8.2 分步迁移与换表

```text
若 is_resizing 为真，确保存在 hash_table_next，行数约为 new_total_bins*bin_size，行结构与主表同
每次在写操作事务中可推进一个旧 bin：将该 bin 内已占用行读出 key，按新桶数重算 h_bin 后调用插入逻辑写入 next 表，并清空原表槽位
resize_progress 自增
当进度达到旧 total_bins 时，用 RENAME 等原子操作交换表名、删除中间名，并更新元数据
非扩容时插入写在活动表；扩容中写 next 表；查询可先 next 后旧表
```

## 九、数据库部署

运行依赖本机或局域网的 MySQL；客户端已链接库。

### 9.1 设计目标

- 以数据库为存储与计算载体的主要形式，逻辑上保持连续下标与数组一一对应。
- 用事务、唯一约束与行锁支持并发下的正确性。

### 9.2 主表

与 init_table 中建表方式一致，示意如下：

```sql
CREATE TABLE hash_table (
  idx BIGINT PRIMARY KEY,
  key BIGINT UNSIGNED NULL,
  fp SMALLINT UNSIGNED NOT NULL,
  UNIQUE KEY uk_key (key)
);
```

空槽为 key 空且 fp=0。初始化时批量插入各行 idx 从 0 到 capacity_slots-1。

### 9.3 元数据

ht_meta 存 n、k、load_factor、种子、total_bins 及扩展开关与进度。

------

## 十、系统接口与运行要点

### 10.1 HTTP 接口

- GET /health
- POST /api/init  体含 n、k、load_factor，可选三枚种子；会重建表与元数据行
- POST /api/insert  POST /api/find  POST /api/erase
- GET /api/stats  GET /api/snapshot  GET /api/bins
- POST /api/batch_insert  POST /api/query_test
- GET /api/experiment/kick_depth_hist
- POST /api/experiment/probe_vs_n  POST /api/experiment/fallback_vs_load  注意部分实验会多次 re-init 表，属破坏性

具体请求体与字段名以 http_server.cpp 为准。

### 10.2 运行环境变量

数据库：DB_HOST、DB_PORT、DB_USER、DB_PASSWORD、DB_NAME。

服务：PORT、BIND_HOST。可选 LOG_FILE 写文件日志。

### 10.3 与论文参数、实验的对应

```text
n：常见 1e4 到 1e6，在接口上限内可调
k：1 到 4 便于对照理论，也可试更大 k 看实现
load_factor：0.7 到 0.99
数据分布：批量接口支持 uniform 与实现中的倾斜分布（非必为严格 Zipf）
可统计：平均探针、P99 探针、回退区占比、kick 深度直方图等
```

------

## 十一、前端与展示

- 技术：Vue 3、axios、ECharts 等，具体以 package.json 与 src 为准。
- 功能上通常包括：初始化、单键插入/查询/擦除、批量插入、查询压测、快照与分 bin 统计、部分实验图。
- 结构可视化可基于 /api/snapshot 返回的 idx 与 fp，按 bin 行展开为网格；颜色约定可与原稿白/蓝/黄等一致，由前端实现决定。

与早期仅有 BinStats 草图字样的区别：实现中分 bin 统计至少包含已用槽数、fallback 用数等字段，以接口返回为准。

```text
Grid 结构示意：
横轴可对应 bin 编号或局部窗口
纵轴可对应 bin 内槽位
空与占用、fallback 区在图上分区着色即可
```

------

## 参考实现文件

- 后端主逻辑：backend-cpp/src/ht.cpp、backend-cpp/src/resize.cpp
- 哈希与参数：backend-cpp/src/hash.cpp、include/config.h
- 数据库封装：backend-cpp/src/db.cpp
- HTTP 路由：backend-cpp/src/http_server.cpp
- 启动入口：backend-cpp/src/main.cpp
- 前端入口：frontend-vue 目录下 Vite 工程
